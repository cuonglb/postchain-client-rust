services:
  postgres:
    image: postgres:16
    restart: always
    volumes:
      - postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_INITDB_ARGS: --lc-collate=C.UTF-8 --lc-ctype=C.UTF-8 --encoding=UTF-8
      POSTGRES_DB: postchain
      POSTGRES_USER: postchain
      POSTGRES_PASSWORD: postchain

  blockchain:
    image: "registry.gitlab.com/chromaway/core-tools/chromia-cli/chr:0.26.0"
    command: sh -c "rm -rf src/lib && chr install && chr node start --wipe"
    ports:
      - 7740:7740
    depends_on:
      - postgres
    volumes:
      - ./testDapp:/usr/app
    environment:
      CHR_DB_URL: jdbc:postgresql://postgres/postchain

volumes:
  postgres: